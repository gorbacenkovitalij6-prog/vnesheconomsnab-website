# Инструкция по настройке Telegram бота для форм обратной связи

## Шаг 1: Создайте Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя для вашего бота (например, "ВНЕШЭКОНОМСНАБ Форма")
4. Введите username для бота (например, "vnesheconomsnab_forms_bot")
5. BotFather отправит вам токен бота в формате `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`

**Сохраните этот токен!** Он понадобится на шаге 3.

## Шаг 2: Получите Chat ID

**Вариант А: Получить свой личный Chat ID**
1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему любое сообщение
3. Бот вернет ваш Chat ID (например, `123456789`)

**Вариант Б: Получить Chat ID группы/канала**
1. Добавьте своего бота в группу или канал
2. Сделайте бота администратором (для каналов обязательно)
3. Отправьте сообщение в группу/канал
4. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - Замените `<YOUR_BOT_TOKEN>` на токен из Шага 1
5. Найдите в ответе поле `"chat":{"id":-123456789}` - это Chat ID
   - Для групп Chat ID начинается с `-`
   - Для супергрупп/каналов Chat ID начинается с `-100`

## Шаг 3: Добавьте токен и Chat ID в файл .env.local

1. Откройте файл `.env.local` в корне проекта
2. Замените `your_bot_token_here` на токен из Шага 1
3. Замените `your_chat_id_here` на Chat ID из Шага 2

Пример:
```env
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHAT_ID=123456789
```

Или для группы:
```env
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHAT_ID=-987654321
```

## Шаг 4: Перезапустите dev сервер

После изменения `.env.local` файла нужно перезапустить сервер:

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова
bun run dev
```

## Шаг 5: Протестируйте форму

1. Откройте сайт в браузере
2. Перейдите на страницу "Контакты" или прокрутите до формы обратной связи на главной
3. Заполните форму и нажмите "Отправить заявку"
4. Проверьте, что сообщение пришло в Telegram

## Формат сообщения

Каждая заявка будет приходить в Telegram в следующем формате:

```
🚗 Новая заявка с сайта ВНЕШЭКОНОМСНАБ

👤 Имя: Иван Иванов
📱 Телефон: +7 (900) 123-45-67
🚘 Желаемая марка: BMW X5
💬 Сообщение: Интересует автомобиль в хорошем состоянии

⏰ Время: 07.12.2024, 15:30:00
```

## Важные замечания

1. **Безопасность**: Никогда не публикуйте файл `.env.local` в Git или где-либо еще
2. **Деплой**: При деплое на Netlify добавьте переменные окружения в настройках проекта
3. **Тестирование**: Перед публикацией обязательно протестируйте отправку формы

## Устранение неполадок

### Сообщения не приходят
- Проверьте правильность токена и Chat ID
- Убедитесь, что бот добавлен в группу/канал (если используется)
- Убедитесь, что бот имеет права администратора (для каналов)
- Проверьте консоль браузера на наличие ошибок

### Ошибка 400 Bad Request
- Проверьте формат токена бота
- Убедитесь, что Chat ID указан правильно (с минусом для групп)

### Ошибка 403 Forbidden
- Бот должен быть добавлен в группу/канал
- Бот должен иметь права на отправку сообщений
